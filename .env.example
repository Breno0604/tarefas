# Provider de persistência do app.
#   local (padrão) — dados no localStorage do navegador
#   api            — FutureApiProvider (stub, ainda não implementado; cairá no seed)
VITE_STORAGE_PROVIDER=local
